namespace SchoolManagement.Infrastructure.Data.Configurations;

// Replaced by AcademicLevel / StudentGroup. File kept empty so the old mapping is not applied.
internal static class RetiredSchoolClassConfiguration;
