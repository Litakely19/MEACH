<!--
    Pager shared by every paged screen. It binds to the ambient data context, so
    any PagedListViewModel can host it without extra wiring.
-->
<UserControl x:Class="SchoolManagement.WPF.Views.Controls.PagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="0,10,0,0">
        <TextBlock Text="{Binding PageSummary}"
                   Style="{StaticResource CaptionStyle}"
                   VerticalAlignment="Center" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Previous"
                    Command="{Binding PreviousPageCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    MinWidth="90" />

            <TextBlock Style="{StaticResource CaptionStyle}" Margin="12,0" VerticalAlignment="Center">
                <Run Text="Page" />
                <Run Text="{Binding Page, Mode=OneWay}" />
                <Run Text="/" />
                <Run Text="{Binding TotalPages, Mode=OneWay}" />
            </TextBlock>

            <Button Content="Next"
                    Command="{Binding NextPageCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    MinWidth="90" />
        </StackPanel>
    </Grid>
</UserControl>
