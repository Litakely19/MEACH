<UserControl x:Class="SchoolManagement.WPF.Views.Administration.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ToolbarPanelStyle}" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             MinWidth="240"
                             Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Search by user name, first name or last name">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding SearchCommand}" />
                        </TextBox.InputBindings>
                    </TextBox>

                    <Button Grid.Column="1"
                            Content="Search"
                            Margin="10,0,0,0"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource SecondaryButtonStyle}" />
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="New user"
                            Command="{Binding CreateCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            MinWidth="130" />
                    <Button Content="Edit" Command="{Binding EditCommand}" Margin="10,0,0,0" />
                    <Button Content="Reset password" Command="{Binding ResetPasswordCommand}" Margin="10,0,0,0" />
                    <Button Content="{Binding ToggleActiveLabel}"
                            Command="{Binding ToggleActiveCommand}"
                            Style="{StaticResource DangerButtonStyle}"
                            Margin="10,0,0,0"
                            MinWidth="120" />
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="10,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="12">
            <Grid>
                <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}">
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditCommand}" />
                    </DataGrid.InputBindings>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="User name" Binding="{Binding Username}" Width="150" />
                        <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*" />
                        <DataGridTextColumn Header="Role" Binding="{Binding RoleDisplayName}" Width="150" />
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200" />
                        <DataGridTextColumn Header="Last sign in"
                                            Binding="{Binding LastLoginAt, StringFormat=g}"
                                            Width="150" />
                        <DataGridTemplateColumn Header="Account" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Active"
                                                   Foreground="{StaticResource SuccessBrush}"
                                                   FontWeight="SemiBold"
                                                   FontSize="12"
                                                   Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <TextBlock Text="Disabled"
                                                   Foreground="{StaticResource DangerBrush}"
                                                   FontWeight="SemiBold"
                                                   FontSize="12"
                                                   Visibility="{Binding IsActive, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Password" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="Change required"
                                               Foreground="{StaticResource WarningBrush}"
                                               FontSize="12"
                                               Visibility="{Binding MustChangePassword, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="No user account matches this search."
                           Style="{StaticResource EmptyStateStyle}"
                           Visibility="{Binding Users.Count, Converter={StaticResource ZeroToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
