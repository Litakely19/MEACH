<UserControl x:Class="SchoolManagement.WPF.Views.Dialogs.StudentEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="3"
                Background="{StaticResource SurfaceAltBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,0,0,14">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Student number:" Style="{StaticResource FieldLabelStyle}" Margin="0,0,8,0" />
                <TextBlock Text="{Binding StudentNumberLabel}" FontWeight="SemiBold" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Grid.Column="0">
            <TextBlock Text="First name" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" />
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="2">
            <TextBlock Text="Last name" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" />
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,12,0,0">
            <TextBlock Text="Gender" Style="{StaticResource FieldLabelStyle}" />
            <ComboBox ItemsSource="{Binding Genders}"
                      SelectedItem="{Binding Gender}"
                      ItemTemplate="{StaticResource EnumItemTemplate}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,12,0,0">
            <TextBlock Text="Date of birth" Style="{StaticResource FieldLabelStyle}" />
            <DatePicker SelectedDate="{Binding DateOfBirth}" />
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,12,0,0">
            <TextBlock Text="Phone number" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" MaxLength="30" />
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,12,0,0">
            <TextBlock Text="Email" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="120" />
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Margin="0,12,0,0">
            <TextBlock Text="Address" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                     MaxLength="250"
                     AcceptsReturn="True"
                     Height="54"
                     TextWrapping="Wrap"
                     VerticalContentAlignment="Top" />
        </StackPanel>

        <StackPanel Grid.Row="5" Grid.Column="0" Margin="0,12,0,0">
            <TextBlock Text="Academic level" Style="{StaticResource FieldLabelStyle}" />
            <ComboBox ItemsSource="{Binding Levels}"
                      SelectedItem="{Binding SelectedLevel}"
                      DisplayMemberPath="DisplayName" />
        </StackPanel>

        <StackPanel Grid.Row="5" Grid.Column="2" Margin="0,12,0,0">
            <TextBlock Text="Student group" Style="{StaticResource FieldLabelStyle}" />
            <ComboBox ItemsSource="{Binding Groups}"
                      SelectedItem="{Binding SelectedGroup}"
                      DisplayMemberPath="DisplayName" />
        </StackPanel>

        <Grid Grid.Row="6" Grid.ColumnSpan="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="School year" Style="{StaticResource FieldLabelStyle}" />
                <ComboBox ItemsSource="{Binding SchoolYears}"
                          SelectedItem="{Binding SelectedSchoolYear}"
                          DisplayMemberPath="DisplayName" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Enrollment date" Style="{StaticResource FieldLabelStyle}" />
                <DatePicker SelectedDate="{Binding EnrollmentDate}" />
            </StackPanel>

            <StackPanel Grid.Column="4">
                <TextBlock Text="Status" Style="{StaticResource FieldLabelStyle}" />
                <ComboBox ItemsSource="{Binding Statuses}"
                          SelectedItem="{Binding Status}"
                          ItemTemplate="{StaticResource EnumItemTemplate}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
