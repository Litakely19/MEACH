<UserControl x:Class="SchoolManagement.WPF.Views.Dialogs.AcademicLevelEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel>
        <TextBlock Text="Name" Style="{StaticResource FieldLabelStyle}" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="40" />

        <TextBlock Text="Description" Style="{StaticResource FieldLabelStyle}" Margin="0,14,0,4" />
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 MaxLength="250"
                 AcceptsReturn="True"
                 Height="70"
                 TextWrapping="Wrap"
                 VerticalContentAlignment="Top" />

        <CheckBox Content="Level is active"
                  IsChecked="{Binding IsActive}"
                  Margin="0,14,0,0"
                  Visibility="{Binding CanChangeActivity, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </StackPanel>
</UserControl>
