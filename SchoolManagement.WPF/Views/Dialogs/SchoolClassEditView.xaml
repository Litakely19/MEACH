<UserControl x:Class="SchoolManagement.WPF.Views.Dialogs.SchoolClassEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Group name" Style="{StaticResource FieldLabelStyle}" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Academic level" Style="{StaticResource FieldLabelStyle}" />
                <ComboBox ItemsSource="{Binding Levels}"
                          SelectedItem="{Binding SelectedLevel}"
                          DisplayMemberPath="DisplayName" />
            </StackPanel>
        </Grid>

        <TextBlock Text="Description" Style="{StaticResource FieldLabelStyle}" Margin="0,14,0,4" />
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 MaxLength="250"
                 AcceptsReturn="True"
                 Height="60"
                 TextWrapping="Wrap"
                 VerticalContentAlignment="Top" />

        <CheckBox Content="Class open for enrollments"
                  IsChecked="{Binding IsActive}"
                  Margin="0,14,0,0"
                  Visibility="{Binding CanChangeActivity, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </StackPanel>
</UserControl>
