<Window x:Class="SchoolManagement.WPF.Views.Dialogs.DialogHostWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="{Binding DialogWidth}"
        SizeToContent="Height"
        MinHeight="200"
        MaxHeight="820"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{StaticResource SurfaceBrush}"
        FocusManager.FocusedElement="{Binding ElementName=DialogContent}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="18,14">
            <TextBlock Text="{Binding Title}"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="SemiBold" />
        </Border>

        <Border Grid.Row="1"
                Style="{StaticResource ErrorBannerStyle}"
                Margin="18,14,18,0"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{StaticResource DangerBrush}"
                       TextWrapping="Wrap" />
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="18,14">
            <ContentControl x:Name="DialogContent" Content="{Binding}" />
        </ScrollViewer>

        <Border Grid.Row="3" Background="{StaticResource SurfaceAltBrush}" Padding="18,12">
            <Grid>
                <ProgressBar HorizontalAlignment="Left"
                             Width="120"
                             Height="4"
                             IsIndeterminate="True"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding CancelButtonText}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            IsCancel="True"
                            MinWidth="110"
                            Command="{Binding CancelCommand}" />
                    <Button Content="{Binding ConfirmButtonText}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="10,0,0,0"
                            MinWidth="130"
                            IsDefault="True"
                            Visibility="{Binding IsEditable, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Command="{Binding ConfirmCommand}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
