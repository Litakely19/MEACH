<UserControl x:Class="SchoolManagement.WPF.Views.Dialogs.SchoolYearEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel>
        <TextBlock Text="Name" Style="{StaticResource FieldLabelStyle}" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="20" />

        <Grid Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Start date" Style="{StaticResource FieldLabelStyle}" />
                <DatePicker SelectedDate="{Binding StartDate}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="End date" Style="{StaticResource FieldLabelStyle}" />
                <DatePicker SelectedDate="{Binding EndDate}" />
            </StackPanel>
        </Grid>

        <CheckBox Content="Use as the current school year"
                  IsChecked="{Binding SetAsCurrent}"
                  Margin="0,16,0,0" />

        <TextBlock Text="The months between the start date and the end date define the monthly scholar fee periods."
                   Style="{StaticResource CaptionStyle}"
                   TextWrapping="Wrap"
                   Margin="0,12,0,0" />
    </StackPanel>
</UserControl>
